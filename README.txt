This is a single-file website. The real scooter photo is embedded directly inside index.html, so no images folder is needed.

To publish: replace your current index.html with this file, commit changes, wait 1-3 minutes, then hard refresh.
